# ai-image-captioning

This project implements an AI system for generating descriptive captions from images. It utilizes various machine learning models and tools to process image data and output natural language descriptions.

## Setup & Usage
To format the project files, run:
```bash
make format
```

## Project Structure
The core logic resides in the `src/` directory, while configuration and model definitions are handled by files like `Modelfile`.